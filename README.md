# capstone-project

## Vercel Deployment

The Next.js app lives in [impactful/](impactful), not the repository root.

When deploying to Vercel, set the project Root Directory to `impactful` so the app router is detected correctly and the deployment does not return `404 NOT_FOUND`.